name: First sample

on:
 push:
  branches: [ "main" ]
 pull_request:
  branches: [ "main" ]
 

jobs:
 build:
  runs-on: ubuntu-latest
  steps:
   - uses: actions/checkout@v4
   
   - name: My name
     run: echo "MY NAME IS 'PAVAN'...!"
     
   - name: Date
     run: |
          echo "Today's Date"
          date
     
   - name: My current directory
     run: |
          echo "current directory"
          pwd
   
