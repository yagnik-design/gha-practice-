name: First sample

on:
 push:
  branches: [ "main", "dev" ]
  paths:
    - 'src/**'
    - '*.yml'
 pull_request:
  branches: [ "main" ]
 schedule:
   - cron: '0 9 * * 1'

jobs:
 build:
  runs-on: ubuntu-latest
  steps:
   - name: My name
     run: echo "MY NAME IS 'PAVAN'...!"
     
   - name: Date
     run: |
          echo "Today's Date"
          date
     
   - name: My current directory
     run: |
          echo "current directory"
          pwd
   
