name: First sample

on:
 push:
  branches: [ "main" ]
 pull_request:
  branches: [ "main" ]
 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Try to reveal secret
        run: echo "Secret is ${{ secrets.MY_SECRET }}"
